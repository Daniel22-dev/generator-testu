{
  "name": "generator-testu",
  "version": "7.0.1",
  "description": "Generátor testů pro učitele – build systém",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check:versions": "node scripts/check-versions.mjs",
    "test": "node scripts/check-versions.mjs && node scripts/check-production-readiness.mjs && node scripts/check-source-structure.mjs && node scripts/check-sensitive-data.mjs && npm run lint && node scripts/build.mjs && npm run test:workflow",
    "lint": "eslint .",
    "check:structure": "node scripts/check-source-structure.mjs",
    "check:sensitive": "node scripts/check-sensitive-data.mjs",
    "check:production": "node scripts/check-production-readiness.mjs",
    "test:workflow": "node tools/workflow-matrix-check.mjs dist/index.html",
    "test:headless": "node tools/headless-check.mjs dist/index.html"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "eslint": "^10.6.0",
    "jsdom": "^29.1.1"
  }
}
